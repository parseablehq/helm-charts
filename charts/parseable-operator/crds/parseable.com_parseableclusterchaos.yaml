---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: parseableclusterchaos.parseable.com
spec:
  group: parseable.com
  names:
    kind: ParseableClusterChaos
    listKind: ParseableClusterChaosList
    plural: parseableclusterchaos
    singular: parseableclusterchaos
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired behavior and configuration of the
              autoscaler.
            properties:
              age:
                format: int64
                type: integer
              cpuUsage:
                type: number
              nodeSelectorLabels:
                additionalProperties:
                  type: string
                type: object
            required:
            - age
            - cpuUsage
            - nodeSelectorLabels
            type: object
          status:
            description: Status reflects the current observed state of the autoscaler.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
