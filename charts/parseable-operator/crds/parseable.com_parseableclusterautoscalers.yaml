---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: parseableclusterautoscalers.parseable.com
spec:
  group: parseable.com
  names:
    kind: ParseableClusterAutoscaler
    listKind: ParseableClusterAutoscalerList
    plural: parseableclusterautoscalers
    singular: parseableclusterautoscaler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ParseableClusterAutoscaler is the Schema for the parseableclusterautoscalers API.
          It represents a custom resource for managing autoscaling behavior in a Kubernetes cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired behavior and configuration of the
              autoscaler.
            properties:
              scaleTargetRef:
                description: ScaleTargetRef specifies the target resource that the
                  autoscaler should manage.
                properties:
                  apiVersion:
                    description: ApiVersion is the version of the API to use with
                      the target resource.
                    type: string
                  kind:
                    description: Kind is the type of resource being referenced (e.g.,
                      Deployment, StatefulSet).
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              scalingConfig:
                description: ScalingConfig contains the configuration for scaling
                  behavior.
                items:
                  description: ScalingConfig defines the scaling behavior for a specific
                    node type or set of nodes.
                  properties:
                    behavior:
                      description: Behavior defines the specific behavior for scaling
                        up and down.
                      properties:
                        scaleDown:
                          description: ScaleDown defines the behavior for scaling
                            down (reducing replicas).
                          properties:
                            policies:
                              description: Policies is an optional set of policies
                                that further define scale down behavior.
                              items:
                                description: Policies define specific rules for scaling
                                  actions, such as scaling by percentage or absolute
                                  number.
                                properties:
                                  type:
                                    description: Type specifies the type of scaling
                                      (e.g., Percentage, Absolute).
                                    type: string
                                  value:
                                    description: Value is the value associated with
                                      the scaling type (e.g., 10% or 5 replicas).
                                    format: int32
                                    type: integer
                                required:
                                - type
                                - value
                                type: object
                              type: array
                          type: object
                        scaleUp:
                          description: ScaleUp defines the behavior for scaling up
                            (increasing replicas).
                          properties:
                            policies:
                              description: Policies is an optional set of policies
                                that further define scale up behavior.
                              items:
                                description: Policies define specific rules for scaling
                                  actions, such as scaling by percentage or absolute
                                  number.
                                properties:
                                  type:
                                    description: Type specifies the type of scaling
                                      (e.g., Percentage, Absolute).
                                    type: string
                                  value:
                                    description: Value is the value associated with
                                      the scaling type (e.g., 10% or 5 replicas).
                                    format: int32
                                    type: integer
                                required:
                                - type
                                - value
                                type: object
                              type: array
                          type: object
                      required:
                      - scaleDown
                      - scaleUp
                      type: object
                    maxReplicas:
                      description: MaxReplicas is the maximum number of replicas the
                        autoscaler can scale to.
                      format: int32
                      type: integer
                    minReplicas:
                      description: MinReplicas is the minimum number of replicas the
                        autoscaler should maintain.
                      format: int32
                      type: integer
                    nodeType:
                      description: NodeType is the type of node to be scaled (e.g.,
                        worker, master).
                      type: string
                    selectorLabels:
                      additionalProperties:
                        type: string
                      description: SelectorLabels are the labels used to identify
                        the nodes to scale.
                      type: object
                    stabilizationWindowSeconds:
                      description: StabilizationWindowSeconds is the time window for
                        stabilizing scaling up/down actions.
                      format: int64
                      type: integer
                    threshold:
                      description: Threshold is the metric threshold that triggers
                        scaling.
                      type: number
                  required:
                  - behavior
                  - maxReplicas
                  - minReplicas
                  - nodeType
                  - selectorLabels
                  - stabilizationWindowSeconds
                  - threshold
                  type: object
                type: array
              stop:
                description: stop reconcilation
                type: boolean
            required:
            - scaleTargetRef
            - scalingConfig
            type: object
          status:
            description: Status reflects the current observed state of the autoscaler.
            properties:
              ingestorIndex:
                additionalProperties:
                  type: string
                type: object
              lastAddStsTimeStamp:
                format: date-time
                type: string
              lastRemoveStsTimeStamp:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
